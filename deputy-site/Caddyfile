# Скопируйте в /etc/caddy/Caddyfile или подключите через import.
# Путь root должен совпадать с DEPLOY_PATH в install.sh / update.sh.
# 
osadchuk.ugrapark.ru {
	root * /var/www/oam.ugrapark.ru
	file_server
	encode gzip zstd
}
